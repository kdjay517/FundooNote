import {Dimensions} from 'react-native';
import {heightPercentage, widthPercentage} from './DynamicDimensions';
const fontscale = Dimensions.get('window').fontScale;

export const COLOR = {
  TEXT_COLOR: 'black',
};

export const SIZES = {
  TEXT_FONT: 32 / fontscale,
};

export const PADDING = {};

export const MARGIN = {};

export const BORDER = {};

export const WIDTH = {};

export const HEIGHT = {};
